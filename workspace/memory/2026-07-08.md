# 2026-07-08

## Magi-17 paper trading / strategy engine
- User asked why Magi-17 had so few trades despite multiple strategies/variants. Diagnosis: many evaluations but gates blocked almost everything; recent stats included ~70k full evaluations, ~13k top evaluations, 31 total paper orders, only 6 clean `passed_strategy_signal`, and no tradable markets.
- Senior trader guidance given: separate live gates from paper research, keep hard economic/risk blockers, allow controlled paper-only exploration for soft blockers, force samples per strategy/variant, tag sample origins, and report funnel metrics.
- User said "Doe het". Implemented and pushed a paper-only research route for Magi-17:
  - `clean_signal` remains separate for live/readiness stats.
  - New `relaxed_research_sample` route creates paper samples when only soft strategy/readiness blockers apply.
  - Hard blockers remain hard: risk tolerance failure, target below costs, net target too low, cost/gross too high.
  - Readiness/shadow-only blockers can be bypassed only for research samples, not clean/live.
  - Reporting now splits `passed_strategy_signal`, `relaxed_research_sample`, fallback, and other.
- Validation after change: `node --check backend/src/server.js` OK; backend rebuilt/restarted; `POST /api/trading-engine/paper/refresh-all` immediately produced 5 planned `relaxed_research_sample` orders, one per top market; health API healthy; no backend `npm test` script available.
- Pushed commit: `7229e4260f8fc8807badeca9c97984fdeda27f4b` to `origin/pushfix-main`.
- Unrelated dirty files noted at the time: `angels/core/openclaw` and `central-dogma/magi-15/backend/src/server.js`.

## Magi-17 Telegram cron/reporting context
- There is a scheduled Telegram update request every 2h for Magi-17 strategy/trading paper status in Dutch.
- It uses local API `http://localhost:5177`; update flow: first `POST /api/trading-engine/paper/refresh-all`, then fetch `/api/health`, `/api/trading-engine/scan`, and `/api/self-learning/trading-engine`.
- Required report style: compact scanable Dutch Telegram update, fixed headers, short lines, always explicitly mention real trading disabled unless configured otherwise.
- Typical sections: STATUS, ACCOUNT, PAPER RESULTS, ACTIVE / PLANNED ORDERS, MARKET SCAN, RISK MODEL, BLOCKERS / RECS, OORDEEL.

## OpenClaw visible agents/sessions
- When user asked "Welke agents heb ik nu", visible sessions in this thread were: `main` active; completed sessions `Victor - Magi-15 Review`, `Victor`, `Victor - Magi-12 Review`, `Pro Trader Algo Advisor`, `Noah`.
- Active subagents at that point: none. Session visibility is limited to the current tree.
