# 2026-07-07

## User Preferences
- Before any code/config/project/cron/durable workflow change, ask for explicit approval first. Read-only inspection and analysis are okay without approval.
- After every real code/project-file change, report remaining Codex limit in the final response, using `session_status` when available. If unavailable, say `Codex limit: onbekend`.
- User expected an approve/decline button for approvals, but if unavailable in Default mode, ask plainly in chat and wait for a clear yes/approval.

## OpenClaw Repo State / GitHub
- OpenClaw repo root: `/nerv/angels/core/openclaw` with remote `https://github.com/rchinwk/openclaw.git` / `github.com:rchinwk/openclaw.git`.
- Committed and pushed `workspace/skills/` only to `main` with commit `9fafa05f58b571d4dc2710a330d645e3172e8bcd` (`Add OpenClaw workflow skills`). GitHub `main` matched that hash after verification.
- Pushed skill files: `workspace/skills/codex-limit-after-code-changes/SKILL.md` and `workspace/skills/github-change-discipline/SKILL.md`.
- Other local dirty items were intentionally not included: `README.md`, `config/aida.yaml`, `workspace/memory/...`, and `workspace/reports/`.

## Trading Cron Preferences
- Magi-15/Magi-17 paper trading updates should be compact, scan-friendly Telegram-style Dutch status reports with fixed section headers and short lines.
- Magi-17 update must call local API `POST http://localhost:5177/api/trading-engine/paper/refresh-all` before fetching `/api/health`, `/api/trading-engine/scan`, and `/api/self-learning/trading-engine`; always state real trading is disabled unless otherwise configured.
# 2026-07-07

## User Preferences
- Before any code/config/project/cron/durable workflow change, ask for explicit approval first. Read-only inspection and analysis are okay without approval.
- After every real code/project-file change, report remaining Codex limit in the final response, using `session_status` when available. If unavailable, say `Codex limit: onbekend`.
- User expected an approve/decline button for approvals, but if unavailable in Default mode, ask plainly in chat and wait for a clear yes/approval.

## OpenClaw Repo State / GitHub
- OpenClaw repo root: `/nerv/angels/core/openclaw` with remote `https://github.com/rchinwk/openclaw.git` / `github.com:rchinwk/openclaw.git`.
- Committed and pushed `workspace/skills/` only to `main` with commit `9fafa05f58b571d4dc2710a330d645e3172e8bcd` (`Add OpenClaw workflow skills`). GitHub `main` matched that hash after verification.
- Pushed skill files: `workspace/skills/codex-limit-after-code-changes/SKILL.md` and `workspace/skills/github-change-discipline/SKILL.md`.
- Other local dirty items were intentionally not included: `README.md`, `config/aida.yaml`, `workspace/memory/...`, and `workspace/reports/`.

## Trading Cron Preferences
- Magi-15/Magi-17 paper trading updates should be compact, scan-friendly Telegram-style Dutch status reports with fixed section headers and short lines.
- Magi-17 update must call local API `POST http://localhost:5177/api/trading-engine/paper/refresh-all` before fetching `/api/health`, `/api/trading-engine/scan`, and `/api/self-learning/trading-engine`; always state real trading is disabled unless otherwise configured.

## Magi Trading Reviews And Changes
- Magi-17 was reviewed as not live-ready: real trading must stay disabled. Current paper sample was 30 closed orders, net PnL about -€63.80, 5W/25L, 3 TP / 7 SL / 20 timeout. The earlier broad strategy stats were polluted by fallback sample orders.
- For Magi-17, user explicitly wants a hard separation: only `execution_mode` / `paper_sample_reason = passed_strategy_signal` may count toward strategy readiness, expectancy, profit factor, and promotion to primary. Fallback, shadow, bootstrap, and legacy orders must be reported separately as research data and must not affect strategy statistics.
- Magi-17 was changed accordingly in `backend/src/server.js`, rebuilt, and restarted in Docker. New API/reporting fields included `paper_orders.strategy_dashboards`, `paper_orders.strategy_readiness`, `paper_orders.research_performance`, `readiness_buckets`, and `research_buckets`. Minimum sample for readiness was set to 30 closed real passed-signal trades. Real trading remained `false`. Not committed at that time.
- Magi-17 live check after the change: total paper orders 30, readiness-eligible 5, research/fallback 25. `volume_node_reversion`: 4 real trades, 1W/3L/2TO, net about -0.09R, PF 0.91, status `collecting_sample`. `trend_pullback_continuation`: 1 real trade, 0W/1L/1TO, net about -0.25R, PF 0, status `collecting_sample`.
- Verification after Magi-17 change: `node --check backend/src/server.js` OK, `git diff --check` OK, Docker rebuild/restart OK, health API healthy, refresh/scan OK. Codex limit reported then: 5h 62% left, week 83% left.
- Magi-15 review: not live-ready. Primary paper sample was 71 closed trades, PnL about -€120.92, expectancy -€1.70/trade, WR 35.2%, PF 0.47. Walk-forward test was worse than train: test 22 trades, WR 27.3%, exp -0.59R, PF 0.31.
- Magi-15 proposed narrowing: disable `breakout_retest` and `volatility_breakout` from primary; keep `scout_touch_rejection` shadow until PF > 1 on a new sample; primary only `strict_sweep_reclaim` and possibly `momentum_continuation`; add primary stop-distance filter min 0.5% and max 1.0%; do not live until at least 30 test trades and PF > 1.1.
