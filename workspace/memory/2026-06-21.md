# 2026-06-21

## Magi-12 / Bitvavo project
- User is working on `magi-12`, a Bitvavo crypto strategy/research dashboard running at `http://localhost:5174`.
- User prefers explanations in Dutch and wants plain, practical meaning, not jargon. They were confused by labels like “Forward Paper Engine” and asked for clearer UI sections.
- Recent implemented state from prior turn: Magi-12 has a paper trading lifecycle engine with statuses `pending`, `open`, `closed_win`, `closed_loss`, `closed_timeout`/timeout, and stores data in `backend/data/forward-paper-trades.json`.
- UI was reorganized into clear sections: `Historical Backtest / Audit`, `Baseline Tests`, `Forward Paper Engine`, `Trade-Ready Gates`, and `Execution And Risk Gaps`.
- A backend background loop was added so paper trading progresses even when the dashboard is not open. It runs every `60s`, refreshes markets, and exposes an `Engine loop` card in the Forward Paper Engine section showing `Running`/`Idle`, last run, duration, errors, and successful markets.
- Last reported engine status: enabled, interval `60s`, first run completed in `62.4s`, errors `0`, markets successful: `BTC-EUR`, `ETH-EUR`, `SOL-EUR`, `XRP-EUR`, `ADA-EUR`.
- Last reported paper metrics after background loop: observed setups increased from `53` to `89`, pending setups `85`, open paper trades `1`, closed paper trades `3`, wins/timeouts `2 / 1`, paper PnL `+26.03`, closed paper winrate `66.67%`.
- Prior performance snapshot: backtest/audit over BTC/ETH/SOL/XRP/ADA had `53` eligible setups, `4` triggered closed trades, wins/losses `2/2`, winrate `50%`, net PnL `+15.28`, expectancy `+3.82/trade`, max drawdown `-9.18`, status `not_trade_ready`.
- Important framing given to user: Magi-12 is currently a research + paper trading dashboard, not ready for real-money live trading. Need at least `50` forward paper closed trades, preferably `100+`, before treating metrics as meaningful.
- CPU status previously checked: Magi-12 containers were very light (`bitvavo-backend-12` around `0.00% CPU`, `110.8 MiB RAM`; `bitvavo-frontend-12` around `0.27% CPU`, `37.9 MiB RAM`). Host pressure seemed memory/swap related (`kswapd0`), not Magi CPU.
- User asked to contact Victor for review; agent-to-agent messaging returned `Agent-to-agent messaging is disabled`. Any “Victor would say” answer should be framed as an inference from earlier reviews, not a live response.
# 2026-06-21

## Magi-12 / Bitvavo project
- User is working on `magi-12`, a Bitvavo crypto strategy/research dashboard running at `http://localhost:5174`.
- User prefers explanations in Dutch and wants plain, practical meaning, not jargon. They were confused by labels like “Forward Paper Engine” and asked for clearer UI sections.
- Recent implemented state from prior turn: Magi-12 has a paper trading lifecycle engine with statuses `pending`, `open`, `closed_win`, `closed_loss`, `closed_timeout`/timeout, and stores data in `backend/data/forward-paper-trades.json`.
- UI was reorganized into clear sections: `Historical Backtest / Audit`, `Baseline Tests`, `Forward Paper Engine`, `Trade-Ready Gates`, and `Execution And Risk Gaps`.
- A backend background loop was added so paper trading progresses even when the dashboard is not open. It runs every `60s`, refreshes markets, and exposes an `Engine loop` card in the Forward Paper Engine section showing `Running`/`Idle`, last run, duration, errors, and successful markets.
- Last reported engine status: enabled, interval `60s`, first run completed in `62.4s`, errors `0`, markets successful: `BTC-EUR`, `ETH-EUR`, `SOL-EUR`, `XRP-EUR`, `ADA-EUR`.
- Last reported paper metrics after background loop: observed setups increased from `53` to `89`, pending setups `85`, open paper trades `1`, closed paper trades `3`, wins/timeouts `2 / 1`, paper PnL `+26.03`, closed paper winrate `66.67%`.
- Prior performance snapshot: backtest/audit over BTC/ETH/SOL/XRP/ADA had `53` eligible setups, `4` triggered closed trades, wins/losses `2/2`, winrate `50%`, net PnL `+15.28`, expectancy `+3.82/trade`, max drawdown `-9.18`, status `not_trade_ready`.
- Important framing given to user: Magi-12 is currently a research + paper trading dashboard, not ready for real-money live trading. Need at least `50` forward paper closed trades, preferably `100+`, before treating metrics as meaningful.
- CPU status previously checked: Magi-12 containers were very light (`bitvavo-backend-12` around `0.00% CPU`, `110.8 MiB RAM`; `bitvavo-frontend-12` around `0.27% CPU`, `37.9 MiB RAM`). Host pressure seemed memory/swap related (`kswapd0`), not Magi CPU.
- User asked to contact Victor for review; agent-to-agent messaging returned `Agent-to-agent messaging is disabled`. Any “Victor would say” answer should be framed as an inference from earlier reviews, not a live response.

## Magi-12 stability fix and latest status
- Magi-12 suffered backend OOM/restarts from heavy paper engine/dashboard runtime work and an oversized support/resistance datafile.
- Implemented fixes: paper engine now uses a lightweight paper-only path instead of rebuilding heavy dashboard/backtest summaries each minute; support/resistance runtime rows are capped; health/status includes uptime, memory, datafile size, runtime rows, paper engine errors, and healthy/degraded state.
- Compacted `backend/data/support-resistance-levels.json` from about `83MB` to `3.4MB`; backup: `backend/data/support-resistance-levels.json.backup-20260621T064553Z`.
- Verification after fix: backend stayed `healthy`, restart count `0`, no new OOM logs, memory roughly `64-78MB RSS`, paper engine errors `0`.
- Later Magi-12 paper status checks stayed technically stable but trading was stagnant: triggered trades `4`, closed trades `4`, open trades `0`, pending setups `94`, wins `2`, timeouts `2`, winrate `50%`, paper PnL `+18.53`.
- Recommendation given: do not live trade yet; wait for at least `50` closed paper trades. If closed/open trades remain stuck after many runs, diagnose why the `94` pending setups do not trigger before loosening strategy rules.

## Codex/OpenClaw usage status shared with user
- User asked for Codex limit. Local status showed model `gpt-5.5`, session usage about `121k / 272k tokens`, `44%` used, about `151k` tokens remaining, cache `99% cached`.
- Clarified that local status does not expose OpenAI/Codex billing or account rate limit, only context/session usage.

## Magi-15 created
- User asked: “Kan je magi-15 maken. Gebruik wat je kan leren van magi 12 en maak een nieuwe engine met paper trading, trading management en self learning. Top 5 crypto munt”.
- Created new project at `/nerv/central-dogma/magi-15`, running live alongside Magi-12.
- Magi-15 URL: `http://127.0.0.1:5175`.
- Containers: `bitvavo-backend-15` and `bitvavo-frontend-15`.
- Top 5 configured markets: `BTC-EUR`, `ETH-EUR`, `SOL-EUR`, `XRP-EUR`, `ADA-EUR`.
- Magi-15 includes paper trading lifecycle, trade management fields/actions (entry, stop, target, risk per unit, break-even/protection state, management actions), and a self-learning API at `/api/self-learning`.
- Self-learning currently measures per-market trigger rate, PnL, expectancy, winrate, management states, and produces recommendations; it does not automatically mutate the live strategy.
- Live verification after creation: backend health `healthy`, restart count `0`, engine runs `3`, errors `0`, pending paper setups `11`, open trades `0`, closed trades `0`, frontend returned `200 OK`.
- Important framing: Magi-15 is paper-only and starts from zero evidence; it does not place real trades.
