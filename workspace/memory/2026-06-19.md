
## 2026-06-19 19:51 Europe/Amsterdam - Magi14 crypto bot work

- User is working on `/nerv/central-dogma/magi-14`, a Bitvavo/paper-trading dashboard/backend. They communicate mostly in Dutch and prefer direct action.
- Recent review found the strategy was effectively only `sr-migration-breakout`; support-bounce and resistance-reject were HOLD-only. Main risks were duplicate shorts around same S/R zone, unstable live level IDs, overly optimistic distant targets/RR, misleading short diagnostics, and overlapping ticks.
- Implemented strategy fixes in code before compaction: stable `zoneKey` for levels, duplicate/cooldown by `zoneKey`, one entry per strategy/zone/candle via `entryLocks`, `tickInFlight` guard, bounded targets max `3 ATR` / `2.5%`, separate breakout/breakdown diagnostics, same zone/candle locking in backtest.
- Rebuilt/recreated backend container after those fixes. Verification at that time: `bitvavo-backend-14` restarted, `/api/health` ok, paper loop running, portfolio intact around `€9995.11`, open trades `0`, latest decision `NO_TRADE`, new output included `zoneKey`, `targetQuality`, and separate diagnostics.
- User asked why nothing happened; live check showed paper loop working, many HOLD ticks, no trades because stricter filters blocked weak/non-fresh setups. User then asked to add top 5 crypto coins and three quality thresholds: “lose, normal, strict”.
- Implemented in code but not yet live/restarted: top-5 supported markets `BTC-EUR`, `ETH-EUR`, `BNB-EUR`, `XRP-EUR`, `SOL-EUR`; `SUPPORTED_MARKETS` env in `docker-compose.yml`; `SR_QUALITY_MODE=loose|normal|strict` with `lose` alias for `loose`; dashboard shows active quality mode.
- Thresholds implemented: `loose`: strength `0.20`, confluence `0.18`, HTF/MTF/LTF matches `1`; `normal`: strength `0.28`, confluence `0.25`, matches `1`; `strict`: strength `0.36`, confluence `0.32`, matches `2`.
- Checks passed for the latest code: backend syntax, `docker compose config -q`, frontend `npm run build`, config smoke test with `SR_QUALITY_MODE=loose`.
- Important caveat to remember: top-5 are only supported markets currently; paper loop still trades a single `DEFAULT_MARKET` at a time, default `BTC-EUR`. Latest top-5/quality-mode changes still need backend/frontend rebuild/restart to go live.

## 2026-06-19 22:07 Europe/Amsterdam - Magi live-deploy preference, Magi14 market UI, Magi13 Reddit-inspired strategy

- User explicitly requested: "Aub altijd meteen life zetten, update het in de md". Durable preference: after normal user-requested code changes, immediately rebuild/restart/deploy live and verify health/status. Still ask before risky actions such as secrets, deletes, public exposure, DB migrations, external paid services, or unclear deploy paths.
- This preference was added to `/nerv/angels/core/openclaw/workspace/AGENTS.md` during the session, despite later flush instructions treating bootstrap/reference files as read-only.
- Magi14 top-5 crypto/quality-mode work was then live-deployed: backend + frontend rebuilt/recreated; `/api/health` OK; `/api/markets` showed `BTC-EUR`, `ETH-EUR`, `BNB-EUR`, `XRP-EUR`, `SOL-EUR`; dashboard showed `qualityMode: normal`; paper loop running; portfolio intact around `€9995.11`, open trades `0`.
- User reported Magi14 UI only showed BTC. Fixed and live-deployed a market dropdown in the dashboard toolbar. `/api/dashboard?market=ETH-EUR` etc. now works per selected market; strategy/SR/backtest use the selected market instead of defaulting to BTC. Status shows `Paper market` because paper loop still trades the configured default (`BTC-EUR`) while dashboard analysis/feed can switch coins.
- Magi14 live verification after dropdown fix: `/api/markets` returned BTC/ETH/BNB/XRP/SOL; sample dashboard prices were BTC-EUR `55065`, ETH-EUR `1485.27`, SOL-EUR `60.41`; backend + frontend were running after rebuild/restart.
- User asked to look at hot Reddit posts around AI/algo trading and build the idea into Magi13. Built and live-deployed in `/nerv/central-dogma/magi-13`.
- Magi13 added strategy `clean-move-striker`: waits during chop, seeks clean range/VWAP breakouts, uses directional efficiency, volume expansion, VWAP, RSI, and range breakout; can SELL when a clean move under VWAP fails.
- Magi13 added trailing-profit protection: enabled by default, activates after `1.2R`, trails by `1 ATR`, and locks at least `0.25R` profit. Dashboard now shows `Trail` in risk controls; README updated.
- Magi13 live verification after deploy: backend health OK, backend + frontend running, paper loop running, `clean-move-striker` visible in `/api/dashboard`, current status `HOLD` with reason `Sitting out chop; no clean move yet`, portfolio intact: equity about `€10116.54`, realized PnL `€118.68`, open trades `0`.
