
## 2026-06-16 13:35 Europe/Amsterdam - Durable Context

- User communicates with Aida through Telegram direct chat. User likes concise, practical updates; Dutch was used for n8n workflow confirmations.
- n8n workflow `Aida Daily Top News` exists and is active. It sends daily at 07:30 Europe/Amsterdam to the user's Telegram chat. It fetches world/top news and Hong Kong news, formats in Dutch, puts Wereldnieuws first and HK nieuws second, top 5 each, with short one-line summaries and compact HTML hyperlinks instead of raw URLs. Important implementation notes: n8n Code node must use `this.helpers.httpRequest(...)`, not global `fetch`; Telegram node uses `parse_mode=HTML`; keep messages compact to avoid Telegram bad-request issues from malformed/overlong HTML.
- n8n workflow `Aida Weekly Buienradar Forecast` exists and is active. It runs every Sunday at 19:00 Europe/Amsterdam and sends a Telegram forecast based on Buienradar official JSON feed `https://data.buienradar.nl/2.0/feed/json`. It includes summary/short-term/long-term text and daily details where available. First scheduled run was set for Sunday 2026-06-21 at 19:00 Amsterdam time.
- User requested an `AIDA google calendar manager workflow` in n8n: they want to ask about availability for relative periods like tomorrow, this week, this weekend; ask it to schedule something with someone in their Google contacts; and receive alerts at a specific time. Work on this had started before compaction with many shell/tool calls, but no final confirmation was sent in the captured context. On resume, inspect n8n workflows/credentials and continue from actual state rather than assuming completion.
- For n8n work, previous pattern was to create/import workflow JSON via local shell, verify with n8n state/logs, restart `core-n8n`, and check logs for `Activated workflow "..."`. Avoid exposing `.env` or token values.
## 2026-06-16 13:35 Europe/Amsterdam - Durable Context

- User communicates with Aida through Telegram direct chat. User likes concise, practical updates; Dutch was used for n8n workflow confirmations.
- n8n workflow `Aida Daily Top News` exists and is active. It sends daily at 07:30 Europe/Amsterdam to the user's Telegram chat. It fetches world/top news and Hong Kong news, formats in Dutch, puts Wereldnieuws first and HK nieuws second, top 5 each, with short one-line summaries and compact HTML hyperlinks instead of raw URLs. Important implementation notes: n8n Code node must use `this.helpers.httpRequest(...)`, not global `fetch`; Telegram node uses `parse_mode=HTML`; keep messages compact to avoid Telegram bad-request issues from malformed/overlong HTML.
- n8n workflow `Aida Weekly Buienradar Forecast` exists and is active. It runs every Sunday at 19:00 Europe/Amsterdam and sends a Telegram forecast based on Buienradar official JSON feed `https://data.buienradar.nl/2.0/feed/json`. It includes summary/short-term/long-term text and daily details where available. First scheduled run was set for Sunday 2026-06-21 at 19:00 Amsterdam time.
- User requested an `AIDA google calendar manager workflow` in n8n: they want to ask about availability for relative periods like tomorrow, this week, this weekend; ask it to schedule something with someone in their Google contacts; and receive alerts at a specific time. Work on this had started before compaction with many shell/tool calls, but no final confirmation was sent in the captured context. On resume, inspect n8n workflows/credentials and continue from actual state rather than assuming completion.
- For n8n work, previous pattern was to create/import workflow JSON via local shell, verify with n8n state/logs, restart `core-n8n`, and check logs for `Activated workflow "..."`. Avoid exposing `.env` or token values.

## 2026-06-16 21:14 Europe/Amsterdam - Calendar/N8n Progress

- The old n8n AI Agent + Ollama approach for Google Calendar was unreliable: Ollama sometimes failed to invoke the Calendar tool and sometimes leaked reasoning. The user accepted replacing it with a fixed n8n backend workflow.
- A fixed n8n workflow named/used as `Aida Calendar Manager` was created and verified. It reads Google Calendar directly via n8n Google Calendar nodes instead of asking an n8n AI Agent to choose tools.
- Calendar target for personal agenda is `Ryan Private`, resolved to calendar id `ryan.chin.wk@gmail.com`. This is the calendar to use for agenda reads/writes unless the user says otherwise.
- Verified agenda reads from `Ryan Private` worked. Upcoming 14-day example on 2026-06-16 included: Groen afval; housekeepers; Papier en karton ophalen; Mees & Teun birthday party; Dansles Cathy; Mia birthday party - Klimbos Fun Forest Amsterdam; Teun Birthday party; Cathy ophalen / naar Lisa brengen; Roque Monkey Town feestje; Restafval.
- User wanted to schedule lunch with Mina Hsu on upcoming Wednesday. Clarification: "in HK" meant HK restaurant in Utrecht, not Hong Kong time. Do not treat HK as timezone in that request. Desired event details were: Wednesday 2026-06-17, 12:00 local Netherlands time, duration 1.5 hours, title `Lunch met Mina Hsu`, location likely HK restaurant in Utrecht, invite Mina if email can be found.
- No lunch event had appeared in the user's calendar at the time they checked. It should not be assumed created. Need verify before creating or editing.
- Google Contacts lookup initially failed because Google People API was disabled for Google Cloud project `1048078794536`. User later enabled People API; subsequent n8n test no longer returned 403, but searching `Mina Hsu` returned no contact match.
- If inviting Mina Hsu, ask for or find her exact email; Google Contacts did not contain a match at last check. Do not create/send an invite with guessed email.
- Suggested Google APIs for broader Aida/n8n use: Calendar API, People API, Gmail API, Tasks API, Drive API, Sheets API, Docs API. For Maps: enable Places API (New), Geocoding API, optionally Directions API, with billing budget/alerts and a restricted API key. Warn that Google Maps Platform may require billing and can incur costs.
- User asked about Codex limits. Current status around 2026-06-16 21:00 Europe/Amsterdam: active model `openai/gpt-5.5`; 5-hour limit about 93% remaining (~7% used); weekly limit about 38% remaining (~62% used); resets about 4h59m for 5-hour and 2d20h for weekly.
